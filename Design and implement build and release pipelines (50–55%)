12/10
-> have a question on cobertura
-- what is cobertura? its a code coverge tool for reporting
-- example yaml
- task: PublishCodeCoverageResults@2
  inputs:
    codeCoverageTool: 'Cobertura'
    summaryFileLocation: 'coverage.xml'

12/10
-> have a question on PMD
-- what is pmd? its a static code analyis tool
-- example yaml
  # Publish PMD results
  - task: PublishBuildArtifacts@1
    inputs:
      pathToPublish: 'target/pmd.xml'
      artifactName: 'pmd-report'
